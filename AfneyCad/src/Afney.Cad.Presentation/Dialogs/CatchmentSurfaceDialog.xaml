<Window x:Class="Afney.Cad.Presentation.Dialogs.CatchmentSurfaceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Yağmur Düşme Alanı — Yüzey Tipi"
        Width="360" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E1E" Foreground="White">

    <Window.Resources>
        <Style x:Key="SurfaceBtn" TargetType="RadioButton">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Ellipse Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0"
                                         Stroke="#888" StrokeThickness="2"
                                         Fill="{TemplateBinding Tag}"/>
                                <ContentPresenter Grid.Column="1" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="{TemplateBinding ToolTip}"
                                           Foreground="#0BB" VerticalAlignment="Center" FontSize="11"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#003355"/>
                                <Setter Property="BorderBrush" Value="#00BBDD"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2A2A3A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <StackPanel Margin="16">
        <!-- Alan adı -->
        <TextBlock Text="Alan Adı:" Margin="0,0,0,4" FontSize="12" Foreground="#AAA"/>
        <TextBox x:Name="AreaNameBox" Text="Çatı Alanı"
                 Background="#2D2D2D" Foreground="White" BorderBrush="#555"
                 Padding="6,5" FontSize="13" Margin="0,0,0,14"/>

        <!-- Alan değeri (bilgi) -->
        <TextBlock x:Name="AreaLabel" FontSize="11" Foreground="#0BB" Margin="0,0,0,10"
                   Text="Çizilen Alan: — m²"/>

        <TextBlock Text="Yüzey Tipi:" Margin="0,0,0,6" FontSize="12" Foreground="#AAA"/>

        <RadioButton x:Name="RbFlat"     Style="{StaticResource SurfaceBtn}" IsChecked="True"
                     Tag="#00BBDD"  ToolTip="C = 1.0"
                     Content="Düz Çatı / Teras"/>
        <RadioButton x:Name="RbSloped"   Style="{StaticResource SurfaceBtn}"
                     Tag="#0077CC"  ToolTip="C = 1.0"
                     Content="Eğimli Çatı"/>
        <RadioButton x:Name="RbPaved"    Style="{StaticResource SurfaceBtn}"
                     Tag="#886633"  ToolTip="C = 0.9"
                     Content="Döşemeli Teras"/>
        <RadioButton x:Name="RbGravel"   Style="{StaticResource SurfaceBtn}"
                     Tag="#888888"  ToolTip="C = 0.7"
                     Content="Çakıl Çatı"/>
        <RadioButton x:Name="RbGreen"    Style="{StaticResource SurfaceBtn}"
                     Tag="#226622"  ToolTip="C = 0.5"
                     Content="Yeşil Çatı"/>

        <!-- Butonlar -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="İptal" Width="80" Margin="0,0,8,0" Click="Cancel_Click"
                    Background="#3A3A3A" Foreground="White" BorderBrush="#555" Padding="0,6"/>
            <Button Content="Kaydet" Width="100" Click="Ok_Click"
                    Background="#005A9C" Foreground="White" BorderBrush="#0077CC" Padding="0,6"
                    FontWeight="Bold"/>
        </StackPanel>
    </StackPanel>
</Window>
