<Window x:Class="Afney.Cad.Presentation.Dialogs.AxonometricExportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="📐 Axonometrik Boru Şeması — 3D İzometrik Export"
        Height="340" Width="520"
        WindowStartupLocation="CenterOwner" Background="#1E1E2E" Foreground="White"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="TextBlock"><Setter Property="Foreground" Value="White"/><Setter Property="VerticalAlignment" Value="Center"/></Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D3F"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/><Setter Property="Padding" Value="4,2"/>
            <Setter Property="Height" Value="26"/><Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2D2D3F"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/><Setter Property="Height" Value="26"/>
                    <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="#252530"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding"    Value="8,5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Bd"
                                            Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}"
                                            SnapsToDevicePixels="True">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#007ACC"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#1F3A5F"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
</Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1565C0"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/><Setter Property="Padding" Value="10,4"/>
            <Setter Property="Height" Value="30"/><Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <DockPanel Margin="16">
        <!-- Başlık -->
        <Border DockPanel.Dock="Top" Background="#0A2040" CornerRadius="4" Padding="10,7" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="📐 Axonometrik Boru Şeması" FontSize="14" FontWeight="Bold" Foreground="#90CAF9"/>
                <TextBlock Text="Kabinetik axonometri projeksiyon · HTML/SVG çıktısı · Pan + Zoom"
                           FontSize="10" Foreground="#5577AA" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>

        <!-- Alt butonlar -->
        <Grid DockPanel.Dock="Bottom" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" Grid.Column="0" Foreground="#77AACC" VerticalAlignment="Center" TextWrapping="Wrap"/>
            <Button Grid.Column="1" Content="📐 Oluştur ve Aç" Width="140" Margin="0,0,8,0" Click="Generate_Click" Background="#1B5E20"/>
            <Button Grid.Column="2" Content="Kapat" Width="80" Click="Close_Click" Background="#333"/>
        </Grid>

        <!-- Parametreler -->
        <Border Background="#0A2040" CornerRadius="4" Padding="14">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Proje Adı:" Margin="0,6"/>
                <TextBox   Grid.Row="0" Grid.Column="1" x:Name="TxtProjectName" Text="Proje" Margin="0,6"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Toplam Kat Sayısı:" Margin="0,6"/>
                <ComboBox  Grid.Row="1" Grid.Column="1" x:Name="CboFloors" Margin="0,6" SelectedIndex="0">
                    <ComboBoxItem Content="1 Kat"/>
                    <ComboBoxItem Content="2 Kat"/>
                    <ComboBoxItem Content="3 Kat"/>
                    <ComboBoxItem Content="4 Kat"/>
                    <ComboBoxItem Content="5 Kat"/>
                    <ComboBoxItem Content="6 Kat"/>
                    <ComboBoxItem Content="8 Kat"/>
                    <ComboBoxItem Content="10 Kat"/>
                </ComboBox>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Kat Yüksekliği (m):" Margin="0,6"/>
                <TextBox   Grid.Row="2" Grid.Column="1" x:Name="TxtFloorHeight" Text="3.0" Margin="0,6"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Çıktı Formatı:" Margin="0,6"/>
                <ComboBox  Grid.Row="3" Grid.Column="1" x:Name="CboFormat" Margin="0,6" SelectedIndex="0">
                    <ComboBoxItem Content="HTML (interaktif, tarayıcıda aç)"/>
                    <ComboBoxItem Content="SVG (vektörel, dosya olarak kaydet)"/>
                </ComboBox>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Tarayıcıda Otomatik Aç:" Margin="0,6"/>
                <CheckBox  Grid.Row="4" Grid.Column="1" x:Name="ChkAutoOpen" IsChecked="True" Margin="0,6">
                    <CheckBox.Style><Style TargetType="CheckBox"><Setter Property="Foreground" Value="White"/></Style></CheckBox.Style>
                </CheckBox>

                <TextBlock Grid.Row="5" Grid.Column="0" Text="Kayıt Yeri:" Margin="0,6"/>
                <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal" Margin="0,6">
                    <TextBox x:Name="TxtSavePath" Width="200" IsReadOnly="True"
                             Text="" Foreground="#888" Background="#1A1A2E" Margin="0,0,6,0"/>
                    <Button Content="📂" Width="36" Click="BrowsePath_Click" Background="#333" Padding="4,2"/>
                </StackPanel>
            </Grid>
        </Border>
    </DockPanel>
</Window>
