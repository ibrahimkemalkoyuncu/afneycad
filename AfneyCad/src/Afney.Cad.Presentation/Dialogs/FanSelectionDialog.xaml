<Window x:Class="Afney.Cad.Presentation.Dialogs.FanSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="💨 Fan Seçimi — Systemair / Halton / S&amp;P Kataloğu"
        Height="700" Width="1100"
        WindowStartupLocation="CenterOwner" Background="#1E1E2E" Foreground="White"
        ResizeMode="CanResize">
    <Window.Resources>
        <Style TargetType="TextBlock"><Setter Property="Foreground" Value="White"/><Setter Property="VerticalAlignment" Value="Center"/></Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D3F"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/><Setter Property="Padding" Value="4,2"/>
            <Setter Property="Height" Value="26"/><Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleBtn" Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press" Focusable="False">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" Data="M0,0 L4,4 L8,0 Z" Fill="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="6,0,20,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Background="#252530" BorderBrush="#555" BorderThickness="1" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" MaxHeight="220">
                                    <ScrollViewer>
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Setter Property="Background" Value="#2D2D3F"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/><Setter Property="Height" Value="26"/>
                    <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="#252530"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding"    Value="8,5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Bd"
                                            Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}"
                                            SnapsToDevicePixels="True">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#007ACC"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#1F3A5F"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
</Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1565C0"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/><Setter Property="Padding" Value="10,4"/>
            <Setter Property="Height" Value="28"/><Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#252535"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/><Setter Property="HorizontalGridLinesBrush" Value="#333"/>
            <Setter Property="CanUserAddRows" Value="False"/><Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="RowBackground" Value="#252535"/><Setter Property="AlternatingRowBackground" Value="#2A2A3E"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#0D3060"/><Setter Property="Foreground" Value="#90CAF9"/>
            <Setter Property="Padding" Value="6,4"/><Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <DockPanel Margin="10">
        <!-- Başlık -->
        <Border DockPanel.Dock="Top" Background="#0D1117" CornerRadius="4" Padding="12,8" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <Path Data="{StaticResource Ico.Fan}" Fill="#FFD740" Width="15" Height="15" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center"/>
                <TextBlock Text="Fan Seçimi" FontSize="15" FontWeight="Bold" Foreground="#FFD740"/>
                <TextBlock Text="  Systemair · Halton · Soler &amp; Palau · EBM-Papst — Q, ΔP, SFP, Ses Analizi"
                           FontSize="10" Foreground="#888" VerticalAlignment="Bottom" Margin="8,0,0,1"/>
            </StackPanel>
        </Border>

        <!-- Alt bar -->
        <Border DockPanel.Dock="Bottom" Padding="0,8,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusText" Grid.Column="0" Foreground="#AAAACC" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Click="SelectAndClose_Click" Background="#1B5E20" Width="130" Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource Ico.CheckCircle}" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" Width="13" Height="13" Margin="0,0,6,0" Stretch="Uniform" VerticalAlignment="Center"/>
                        <TextBlock Text="Seç ve Kapat" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Grid.Column="2" Content="Kapat" Width="80" Click="Close_Click" Background="#333"/>
            </Grid>
        </Border>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Filtre paneli -->
            <Border Grid.Row="0" Background="#0A1020" CornerRadius="4" Padding="12,8" Margin="0,0,0,8">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Debi (m³/h):" Margin="0,0,6,0"/>
                    <TextBox x:Name="TxtFlow" Width="80" Text="500" Margin="0,0,16,0"/>

                    <TextBlock Text="Basınç (Pa):" Margin="0,0,6,0"/>
                    <TextBox x:Name="TxtPressure" Width="80" Text="150" Margin="0,0,16,0"/>

                    <TextBlock Text="Güvenlik Payı Debi (%):" Margin="0,0,6,0"/>
                    <TextBox x:Name="TxtSafetyFlow" Width="50" Text="15" Margin="0,0,16,0"/>

                    <TextBlock Text="Basınç (%):" Margin="0,0,6,0"/>
                    <TextBox x:Name="TxtSafetyPress" Width="50" Text="20" Margin="0,0,16,0"/>

                    <TextBlock Text="Tip:" Margin="0,0,6,0"/>
                    <ComboBox x:Name="CboType" Width="120" Margin="0,0,10,0" SelectedIndex="0">
                        <ComboBoxItem Content="Tümü"/>
                        <ComboBoxItem Content="Aksiyal (Axial)"/>
                        <ComboBoxItem Content="Santrifüj"/>
                        <ComboBoxItem Content="In-line"/>
                        <ComboBoxItem Content="Çatı Fanı"/>
                        <ComboBoxItem Content="ERV / Isı Geri Kaz."/>
                    </ComboBox>

                    <TextBlock Text="Üretici:" Margin="0,0,6,0"/>
                    <ComboBox x:Name="CboManuf" Width="110" Margin="0,0,10,0" SelectedIndex="0">
                        <ComboBoxItem Content="Tümü"/>
                        <ComboBoxItem Content="Systemair"/>
                        <ComboBoxItem Content="Halton"/>
                        <ComboBoxItem Content="Soler &amp; Palau"/>
                        <ComboBoxItem Content="EBM-Papst"/>
                    </ComboBox>

                    <Button Click="Search_Click" Background="#0D47A1" Width="80">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource Ico.Search}" Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" Width="13" Height="13" Margin="0,0,6,0" Stretch="Uniform" VerticalAlignment="Center"/>
                            <TextBlock Text="Ara" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>

            <!-- Sonuç tablosu + detay paneli -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="300"/>
                </Grid.ColumnDefinitions>

                <DataGrid x:Name="FanGrid" Grid.Column="0" AutoGenerateColumns="False"
                          SelectionChanged="FanGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Üretici"    Binding="{Binding Fan.Manufacturer}" Width="100"/>
                        <DataGridTextColumn Header="Model"      Binding="{Binding Fan.ModelName}"    Width="160"/>
                        <DataGridTextColumn Header="Tip"        Binding="{Binding Fan.Type}"         Width="90"/>
                        <DataGridTextColumn Header="Q maks (m³/h)" Binding="{Binding Fan.MaxFlowM3h}" Width="90"/>
                        <DataGridTextColumn Header="ΔP maks (Pa)"  Binding="{Binding Fan.MaxPressurePa}" Width="90"/>
                        <DataGridTextColumn Header="Güç (kW)"   Binding="{Binding Fan.PowerKw}"     Width="70"/>
                        <DataGridTextColumn Header="Verim (%)"  Binding="{Binding Fan.EfficiencyPct}" Width="70"/>
                        <DataGridTextColumn Header="Ses (dB(A))" Binding="{Binding Fan.NoiseDB}"    Width="80"/>
                        <DataGridTextColumn Header="SFP"        Binding="{Binding SFPCategory}"     Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Detay paneli -->
                <Border Grid.Column="2" Background="#0D1117" CornerRadius="4" Padding="12">
                    <StackPanel>
                        <TextBlock Text="FAN DETAYI" Foreground="#FFD740" FontWeight="Bold" FontSize="11" Margin="0,0,0,8"/>
                        <TextBlock x:Name="DetModel"  Text="—" FontSize="14" FontWeight="Bold" Foreground="#FFD740" Margin="0,0,0,4" TextWrapping="Wrap"/>
                        <TextBlock x:Name="DetMaker"  Text="—" Foreground="#90CAF9" Margin="0,0,0,8" TextWrapping="Wrap"/>

                        <Separator Background="#333" Margin="0,0,0,8"/>

                        <Grid Margin="0,0,0,6">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Maks Debi" FontSize="9" Foreground="#888"/>
                                <TextBlock x:Name="DetFlow" Text="—" FontSize="14" FontWeight="Bold" Foreground="#80DEEA"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Maks Basınç" FontSize="9" Foreground="#888"/>
                                <TextBlock x:Name="DetPress" Text="—" FontSize="14" FontWeight="Bold" Foreground="#EF9A9A"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Motor Gücü" FontSize="9" Foreground="#888"/>
                                <TextBlock x:Name="DetPower" Text="—" FontSize="13" FontWeight="Bold" Foreground="#CE93D8"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Verim" FontSize="9" Foreground="#888"/>
                                <TextBlock x:Name="DetEff" Text="—" FontSize="13" FontWeight="Bold" Foreground="#A5D6A7"/>
                            </StackPanel>
                        </Grid>

                        <Separator Background="#333" Margin="0,0,0,8"/>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="SES" FontSize="9" Foreground="#888"/>
                                <TextBlock x:Name="DetNoise" Text="—" FontSize="13" FontWeight="Bold" Foreground="#FFCC02"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="SFP" FontSize="9" Foreground="#888"/>
                                <TextBlock x:Name="DetSFP" Text="—" FontSize="11" Foreground="#80DEEA" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Yedek Payı" FontSize="10" Foreground="#90CAF9" FontWeight="Bold" Margin="0,0,0,4"/>
                        <TextBlock x:Name="DetMargin" Text="—" Foreground="#A5D6A7" FontSize="10" TextWrapping="Wrap" Margin="0,0,0,8"/>

                        <TextBlock Text="Ses Değerlendirmesi" FontSize="10" Foreground="#90CAF9" FontWeight="Bold" Margin="0,0,0,4"/>
                        <TextBlock x:Name="DetNoiseEval" Text="—" Foreground="#FFCC02" FontSize="10" TextWrapping="Wrap" Margin="0,0,0,8"/>

                        <Separator Background="#333" Margin="0,0,0,8"/>
                        <TextBlock Text="Uygulama" FontSize="9" Foreground="#888"/>
                        <TextBlock x:Name="DetApp"   Text="—" FontSize="10" Foreground="#AAA" TextWrapping="Wrap" Margin="0,2,0,4"/>
                        <TextBlock x:Name="DetNotes" Text="—" FontSize="10" Foreground="#888" TextWrapping="Wrap" FontStyle="Italic"/>

                        <Border Background="#0D2020" CornerRadius="3" Padding="6" Margin="0,8,0,0">
                            <StackPanel>
                                <TextBlock Text="Bağlantı / IP / Enerji" FontSize="9" Foreground="#888"/>
                                <TextBlock x:Name="DetConn" Text="—" FontSize="10" Foreground="#AAA"/>
                                <TextBlock x:Name="DetEC" Text="" Foreground="#A5D6A7" FontSize="10"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Özet çubuğu -->
            <Border Grid.Row="2" Background="#0A1020" CornerRadius="4" Padding="10,6" Margin="0,6,0,0">
                <TextBlock x:Name="SummaryText" Foreground="#AAAACC" FontSize="10" TextWrapping="Wrap"/>
            </Border>
        </Grid>
    </DockPanel>
</Window>
