<Window x:Class="Afney.Cad.Presentation.Dialogs.WallParallelRouteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🧱 Duvara Paralel Boru Rotalama — AfneyCAD" Height="580" Width="700"
        WindowStartupLocation="CenterOwner" Background="#1E1E2E" Foreground="White" ResizeMode="CanResize">
    <Window.Resources>
        <Style TargetType="TextBlock"><Setter Property="Foreground" Value="White"/><Setter Property="VerticalAlignment" Value="Center"/></Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D3F"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/><Setter Property="Padding" Value="4,2"/>
            <Setter Property="Height" Value="26"/><Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2D2D3F"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/><Setter Property="Height" Value="26"/>
                    <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="#252530"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding"    Value="8,5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Bd"
                                            Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}"
                                            SnapsToDevicePixels="True">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#007ACC"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#1F3A5F"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
</Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1565C0"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/><Setter Property="Padding" Value="10,4"/>
            <Setter Property="Height" Value="28"/><Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#252535"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/><Setter Property="HorizontalGridLinesBrush" Value="#333"/>
            <Setter Property="RowBackground" Value="#252535"/><Setter Property="AlternatingRowBackground" Value="#2A2A3E"/>
            <Setter Property="CanUserAddRows" Value="False"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#0D3060"/><Setter Property="Foreground" Value="#90CAF9"/>
            <Setter Property="Padding" Value="6,4"/><Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="#90CAF9"/><Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="Padding" Value="8,6"/><Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <DockPanel Margin="10">
        <!-- Başlık -->
        <Border DockPanel.Dock="Top" Background="#0D3060" CornerRadius="4" Padding="10,7" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🧱 Duvara Paralel Boru Rotalama" FontSize="15" FontWeight="Bold" Foreground="#90CAF9"/>
                <TextBlock Text="  Mimari duvarlar boyunca otomatik boru yerleşimi" FontSize="10" Foreground="#7799BB" VerticalAlignment="Bottom" Margin="8,0,0,1"/>
            </StackPanel>
        </Border>

        <!-- Alt buton çubuğu -->
        <Border DockPanel.Dock="Bottom" Padding="0,8,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusText" Grid.Column="0" Text="Duvarları taramak için 'Duvarları Tara' düğmesine basın." Foreground="#99BBDD" VerticalAlignment="Center" TextWrapping="Wrap"/>
                <Button Grid.Column="1" Content="🔨 Rotalama Yap" Width="130" Margin="0,0,8,0"
                        Click="Route_Click" Background="#1B5E20" IsEnabled="False" x:Name="BtnRoute"/>
                <Button Grid.Column="2" Content="İptal" Width="80" Click="Cancel_Click" Background="#333"/>
            </Grid>
        </Border>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Parametreler -->
            <GroupBox Grid.Row="0" Header="Boru Parametreleri">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Duvar Ofseti (mm):" Margin="0,2,8,2"/>
                    <TextBox   Grid.Row="0" Grid.Column="1" x:Name="OffsetInput" Text="150" Margin="0,2,0,2"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Boru Çapı (mm):" Margin="0,2,8,2"/>
                    <TextBox   Grid.Row="1" Grid.Column="1" x:Name="DiameterInput" Text="20" Margin="0,2,0,2"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Sistem Tipi:" Margin="0,2,8,2"/>
                    <ComboBox  Grid.Row="2" Grid.Column="1" x:Name="SystemTypeCombo" Margin="0,2,0,2">
                        <ComboBoxItem Content="Soğuk Su" IsSelected="True"/>
                        <ComboBoxItem Content="Sıcak Su"/>
                        <ComboBoxItem Content="Pis Su"/>
                        <ComboBoxItem Content="Yangın"/>
                    </ComboBox>
                </Grid>
            </GroupBox>

            <!-- Duvar Kaynağı -->
            <GroupBox Grid.Row="1" Header="Duvar Kaynağı">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,6">
                        <RadioButton x:Name="RbAutoDetect" Content="Otomatik Tespit (Mimari Layer)" IsChecked="True"
                                     GroupName="WallSource" Foreground="White" Margin="0,0,16,0"
                                     Checked="WallSource_Changed"/>
                        <RadioButton x:Name="RbManual" Content="Manuel Koordinat Girişi"
                                     GroupName="WallSource" Foreground="White"
                                     Checked="WallSource_Changed"/>
                    </StackPanel>

                    <!-- Otomatik panel -->
                    <StackPanel x:Name="PanelAuto" DockPanel.Dock="Top">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBlock Text="Duvar Layer Filtresi:" Margin="0,0,8,0"/>
                            <ComboBox x:Name="LayerFilter" Width="200" Margin="0,0,8,0"/>
                            <Button Content="🔍 Duvarları Tara" Width="120" Click="ScanWalls_Click" Background="#4A148C"/>
                        </StackPanel>
                        <DataGrid x:Name="WallGrid" Height="130" AutoGenerateColumns="False" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Başlangıç X"  Binding="{Binding StartX, StringFormat=F0}"  Width="90"/>
                                <DataGridTextColumn Header="Başlangıç Y"  Binding="{Binding StartY, StringFormat=F0}"  Width="90"/>
                                <DataGridTextColumn Header="Bitiş X"      Binding="{Binding EndX, StringFormat=F0}"    Width="90"/>
                                <DataGridTextColumn Header="Bitiş Y"      Binding="{Binding EndY, StringFormat=F0}"    Width="90"/>
                                <DataGridTextColumn Header="Kalınlık (mm)" Binding="{Binding Thickness, StringFormat=F0}" Width="90"/>
                                <DataGridTextColumn Header="Uzunluk (m)"  Binding="{Binding LengthM, StringFormat=F2}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>

                    <!-- Manuel panel -->
                    <StackPanel x:Name="PanelManual" Visibility="Collapsed" DockPanel.Dock="Top">
                        <TextBlock Text="Segment koordinatlarını girin (mm cinsinden, noktalı virgülle ayırın):" Margin="0,0,0,4" Foreground="#AAA" FontSize="11"/>
                        <TextBox x:Name="ManualSegments" Height="80" AcceptsReturn="True" TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 Text="0,0 → 5000,0&#10;5000,0 → 5000,4000"
                                 ToolTip="Her satır bir segment: X1,Y1 → X2,Y2"/>
                    </StackPanel>
                </DockPanel>
            </GroupBox>
        </Grid>
    </DockPanel>
</Window>
