<Window x:Class="Afney.Cad.Presentation.Dialogs.ValveLibraryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AfneyCAD — Vana Kütüphanesi (VALVE LIB)" Height="450" Width="600"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#1E1E1E" Foreground="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="MEP Vana Kataloğu" FontSize="18" FontWeight="Bold" Foreground="#00FFBC"/>
            <TextBlock Text="Projenize eklemek istediğiniz vanayı seçin." FontSize="11" Foreground="#AAAAAA"/>
        </StackPanel>

        <DataGrid x:Name="ValveGrid" Grid.Row="1" AutoGenerateColumns="False" 
                  IsReadOnly="True" Background="#252525" Foreground="White"
                  BorderBrush="#333" HeadersVisibility="Column" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Kod" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="Vana Adı" Binding="{Binding NameTR}" Width="*"/>
                <DataGridTextColumn Header="Tip" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="DN (mm)" Binding="{Binding NominalDiameter}" Width="70"/>
                <DataGridTextColumn Header="Zeta (ζ)" Binding="{Binding LossCoefficient}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Vana Özellikleri..." Width="120" Margin="0,0,10,0" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>
            <Button Content="İptal" Width="80" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="Vana Yerleştir" Width="120" Background="#007ACC" Foreground="White" Click="Place_Click"/>
        </StackPanel>
    </Grid>
</Window>
