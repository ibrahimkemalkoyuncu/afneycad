<Window x:Class="Afney.Cad.Presentation.Dialogs.PipeCostDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Boru Maliyet Analizi — AfneyCAD"
        Height="680" Width="940" WindowStartupLocation="CenterOwner"
        Background="#1E1E2E" Foreground="White" ResizeMode="CanResize">
    <Window.Resources>
        <Style TargetType="TextBlock"><Setter Property="Foreground" Value="White"/></Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D3F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="4,2"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2D2D3F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
                    <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="#252530"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding"    Value="8,5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Bd"
                                            Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}"
                                            SnapsToDevicePixels="True">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#007ACC"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#1F3A5F"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
</Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#005A9C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#2D2D3F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#444"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="RowBackground" Value="#252535"/>
            <Setter Property="AlternatingRowBackground" Value="#2A2A3E"/>
            <Setter Property="ColumnHeaderHeight" Value="28"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Başlık -->
        <TextBlock Grid.Row="0" Text="BORU MALİYET ANALİZİ"
                   FontSize="15" FontWeight="Bold" Foreground="#FFDDAA" Margin="0,0,0,12"/>

        <!-- Araç Çubuğu -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="📂 Projeden Yükle" Click="LoadFromDb_Click"/>
            <Button Content="➕ Hat Ekle"        Click="AddRow_Click" Margin="8,0,0,0" Background="#2E8B57"/>
            <Button Content="🗑️ Sil"             Click="DeleteRow_Click" Margin="4,0,0,0" Background="#6B2020"/>
            <TextBlock Text="Beklenmedik Gider (%):" VerticalAlignment="Center" Margin="20,0,8,0"/>
            <TextBox x:Name="TxtContingency" Text="10" Width="45"/>
        </StackPanel>

        <!-- Giriş + Sonuç Tabloları -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="180"/>
            </Grid.RowDefinitions>

            <!-- Giriş Tablosu -->
            <DataGrid Grid.Row="0" x:Name="InputGrid" AutoGenerateColumns="False"
                      CanUserAddRows="False" CanUserDeleteRows="False"
                      HeadersVisibility="Column" CanUserResizeRows="False" Margin="0,0,0,8">
                <DataGrid.Columns>
                    <DataGridTextColumn     Header="Açıklama"      Binding="{Binding Description}"   Width="*"   IsReadOnly="False"/>
                    <DataGridComboBoxColumn Header="Malzeme"       SelectedItemBinding="{Binding Material, UpdateSourceTrigger=PropertyChanged}" Width="110" IsReadOnly="False"/>
                    <DataGridTextColumn     Header="Çap (mm)"      Binding="{Binding DiameterStr}"   Width="75"  IsReadOnly="False"/>
                    <DataGridTextColumn     Header="Uzunluk (m)"   Binding="{Binding LengthStr}"     Width="85"  IsReadOnly="False"/>
                    <DataGridTextColumn     Header="Sistem"        Binding="{Binding System}"        Width="100" IsReadOnly="False"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Sonuç Tablosu -->
            <DataGrid Grid.Row="1" x:Name="ResultGrid" AutoGenerateColumns="False"
                      IsReadOnly="True" HeadersVisibility="Column" CanUserResizeRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Açıklama"      Binding="{Binding Description}"    Width="*"/>
                    <DataGridTextColumn Header="Uzunluk (m)"   Binding="{Binding LengthStr}"      Width="80"/>
                    <DataGridTextColumn Header="Malzeme (TL)"  Binding="{Binding MaterialStr}"    Width="100"/>
                    <DataGridTextColumn Header="İşçilik (TL)"  Binding="{Binding LaborStr}"       Width="100"/>
                    <DataGridTextColumn Header="Ek Parça (TL)" Binding="{Binding FittingStr}"     Width="100"/>
                    <DataGridTextColumn Header="Toplam (TL)"   Binding="{Binding TotalStr}"       Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Özet -->
        <Border Grid.Row="3" Background="#1A1A2E" BorderBrush="#445" BorderThickness="1"
                Padding="10,8" Margin="0,8,0,8">
            <StackPanel>
                <TextBlock x:Name="SummaryText"
                           Text="Hesap için 'Hesapla' butonuna basın."
                           Foreground="#FFD700" FontFamily="Consolas" FontSize="12"/>
                <TextBlock x:Name="NotesText" Foreground="#888" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Butonlar -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="⚙️ Hesapla"     Click="Calculate_Click"  Width="100" FontWeight="Bold" Height="32"/>
            <Button Content="📄 HTML Rapor"  Click="ExportHtml_Click" Width="110" Margin="8,0,0,0" Height="32" Background="#2E8B57"/>
            <Button Content="Kapat"          Click="Close_Click"      Width="70"  Margin="8,0,0,0" Height="32" Background="#444"/>
        </StackPanel>
    </Grid>
</Window>
