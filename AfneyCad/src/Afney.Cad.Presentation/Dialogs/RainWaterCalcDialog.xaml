<Window x:Class="Afney.Cad.Presentation.Dialogs.RainWaterCalcDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Yağmur Suyu Hesap Föyü — TS EN 12056-3"
        Height="680" Width="1050" WindowStartupLocation="CenterOwner"
        Background="#1E1E2E" Foreground="White" ResizeMode="CanResize">
    <Window.Resources>
        <Style TargetType="TextBlock"><Setter Property="Foreground" Value="White"/></Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D3F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="26"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#2D2D3F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="Height" Value="26"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1565C0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="30"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#252535"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#333"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="RowBackground" Value="#252535"/>
            <Setter Property="AlternatingRowBackground" Value="#2A2A3E"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#0D3060"/>
            <Setter Property="Foreground" Value="#90CAF9"/>
            <Setter Property="Padding" Value="6,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="#90CAF9"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#CCCCEE"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>
    </Window.Resources>

    <DockPanel Margin="12">
        <!-- Başlık -->
        <Border DockPanel.Dock="Top" Background="#0D3060" CornerRadius="4" Padding="12,8" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Yağmur Suyu Hesap Föyü" FontSize="16" FontWeight="Bold" Foreground="#90CAF9"/>
                <TextBlock Text="  —  TS EN 12056-3 / TS 821" FontSize="12" Foreground="#7799BB" VerticalAlignment="Bottom" Margin="0,0,0,1"/>
            </StackPanel>
        </Border>

        <!-- Alt buton çubuğu -->
        <Border DockPanel.Dock="Bottom" Padding="0,8,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="TxtStatus" Grid.Column="0" Text="Hazır" Foreground="#99BBDD"
                           VerticalAlignment="Center" TextWrapping="Wrap"/>
                <Button Grid.Column="1" Content="HTML Rapor" Width="110" Margin="8,0,0,0" Click="ExportHtml_Click"
                        Background="#1B5E20"/>
                <Button Grid.Column="2" Content="Yenile" Width="80" Margin="8,0,0,0" Click="Calculate_Click"
                        Background="#37474F"/>
                <Button Grid.Column="3" Content="Kapat" Width="80" Margin="8,0,0,0" Click="Close_Click"
                        Background="#333"/>
            </Grid>
        </Border>

        <!-- Ana içerik -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Parametreler -->
            <GroupBox Grid.Row="0" Header="Hesap Parametreleri">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Content="Konum:"/>
                    <ComboBox Grid.Column="1" x:Name="CmbLocation" Margin="0,0,12,0">
                        <ComboBoxItem Content="İstanbul" IsSelected="True"/>
                        <ComboBoxItem Content="Ankara"/>
                        <ComboBoxItem Content="İzmir"/>
                        <ComboBoxItem Content="Bursa"/>
                        <ComboBoxItem Content="Antalya"/>
                        <ComboBoxItem Content="Trabzon"/>
                        <ComboBoxItem Content="Diğer"/>
                    </ComboBox>
                    <Label Grid.Column="2" Content="Yağış Yoğunluğu r (l/s·m²):"/>
                    <TextBox Grid.Column="3" x:Name="TxtRainfall" Text="0.030" Margin="0,0,12,0"/>
                    <Label Grid.Column="4" Content="Eğim (%):"/>
                    <TextBox Grid.Column="5" x:Name="TxtSlope" Text="1.0" Margin="0,0,12,0"/>
                    <Label Grid.Column="6" Content="Boru Malzemesi:"/>
                    <ComboBox Grid.Column="7" x:Name="CmbMaterial" Margin="0,0,12,0">
                        <ComboBoxItem Content="PVC" IsSelected="True"/>
                        <ComboBoxItem Content="PP"/>
                        <ComboBoxItem Content="Dökme Demir"/>
                        <ComboBoxItem Content="Galvaniz"/>
                    </ComboBox>
                    <Button Grid.Column="9" Content="Hesapla" Width="100" Click="Calculate_Click"
                            Background="#1565C0"/>
                </Grid>
            </GroupBox>

            <!-- Sonuç Tablosu -->
            <GroupBox Grid.Row="1" Header="Yağmur Suyu Hesap Tablosu">
                <DataGrid x:Name="CalcGrid" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="No"         Binding="{Binding RowNo}"         Width="40"/>
                        <DataGridTextColumn Header="Alan Adı"   Binding="{Binding AreaName}"       Width="130"/>
                        <DataGridTextColumn Header="Yüzey"      Binding="{Binding SurfaceType}"    Width="100"/>
                        <DataGridTextColumn Header="A (m²)"     Binding="{Binding AreaM2,StringFormat=F1}"      Width="70"/>
                        <DataGridTextColumn Header="C"          Binding="{Binding RunoffC,StringFormat=F2}"     Width="45"/>
                        <DataGridTextColumn Header="r (l/s·m²)" Binding="{Binding RainfallR,StringFormat=F3}"  Width="80"/>
                        <DataGridTextColumn Header="Q (l/s)"    Binding="{Binding DesignFlowLs,StringFormat=F3}" Width="70"/>
                        <DataGridTextColumn Header="DN (mm)"    Binding="{Binding DiameterMm}"    Width="60"/>
                        <DataGridTextColumn Header="Eğim %"     Binding="{Binding SlopePct,StringFormat=F1}"   Width="60"/>
                        <DataGridTextColumn Header="V (m/s)"    Binding="{Binding VelocityMs,StringFormat=F2}" Width="65"/>
                        <DataGridTextColumn Header="Doluluk"    Binding="{Binding FillRatio,StringFormat=P0}"  Width="65"/>
                        <DataGridTextColumn Header="Q_dolu (l/s)" Binding="{Binding CapacityLs,StringFormat=F3}" Width="80"/>
                        <DataGridTextColumn Header="Durum"      Binding="{Binding IsOk}"          Width="55"/>
                        <DataGridTextColumn Header="Uyarılar"   Binding="{Binding Warnings}"      Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <!-- Özet Notlar -->
            <GroupBox Grid.Row="2" Header="Özet" MaxHeight="110">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="PanelNotes"/>
                </ScrollViewer>
            </GroupBox>
        </Grid>
    </DockPanel>
</Window>
