<Window x:Class="Afney.Cad.Presentation.Dialogs.FixtureLibraryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Vitrifiye Kataloğu — AfneyCAD" Height="640" Width="920"
        WindowStartupLocation="CenterOwner" Background="#1E1E2E" Foreground="White" ResizeMode="CanResize">
    <Window.Resources>
        <Style TargetType="TextBlock"><Setter Property="Foreground" Value="White"/><Setter Property="VerticalAlignment" Value="Center"/></Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D3F"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/><Setter Property="Padding" Value="4,2"/>
            <Setter Property="Height" Value="26"/><Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleBtn" Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press" Focusable="False">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" Data="M0,0 L4,4 L8,0 Z" Fill="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="6,0,20,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Background="#252530" BorderBrush="#555" BorderThickness="1" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" MaxHeight="220">
                                    <ScrollViewer>
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Setter Property="Background" Value="#2D2D3F"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/><Setter Property="Height" Value="26"/>
                    <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="#252530"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding"    Value="8,5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Bd"
                                            Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}"
                                            SnapsToDevicePixels="True">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#007ACC"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#1F3A5F"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
</Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1565C0"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/><Setter Property="Padding" Value="10,4"/>
            <Setter Property="Height" Value="30"/><Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#252535"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/><Setter Property="HorizontalGridLinesBrush" Value="#333"/>
            <Setter Property="IsReadOnly" Value="True"/><Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="RowBackground" Value="#252535"/><Setter Property="AlternatingRowBackground" Value="#2A2A3E"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#0D3060"/><Setter Property="Foreground" Value="#90CAF9"/>
            <Setter Property="Padding" Value="6,4"/><Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <DockPanel Margin="10">
        <!-- Başlık -->
        <Border DockPanel.Dock="Top" Background="#0D3060" CornerRadius="4" Padding="10,7" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <Path Data="{StaticResource Ico.Wrench}" Fill="#90CAF9" Width="15" Height="15" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center"/>
                <TextBlock Text="Vitrifiye Kataloğu" FontSize="15" FontWeight="Bold" Foreground="#90CAF9"/>
                <TextBlock Text="  TS 1258 / DIN 1988 / TS EN 12056" FontSize="10" Foreground="#7799BB" VerticalAlignment="Bottom" Margin="0,0,0,1"/>
            </StackPanel>
        </Border>

        <!-- Alt buton çubuğu -->
        <Border DockPanel.Dock="Bottom" Padding="0,8,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="TxtStatus" Grid.Column="0" Text="" Foreground="#77AA77" VerticalAlignment="Center"/>
                <Button x:Name="BtnInsert" Grid.Column="1" Content="+ Çizime Ekle" Width="120" Margin="0,0,8,0"
                        Click="Insert_Click" Background="#1B5E20" IsEnabled="False"/>
                <Button Grid.Column="2" Content="Kapat" Width="80" Click="Close_Click" Background="#333"/>
            </Grid>
        </Border>

        <!-- Filtre çubuğu -->
        <Border DockPanel.Dock="Top" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Kategori:" Margin="0,0,6,0"/>
                <ComboBox x:Name="CategoryFilter" Width="160" SelectionChanged="CategoryFilter_Changed" Margin="0,0,16,0">
                    <ComboBoxItem Content="Tümü" IsSelected="True"/>
                    <ComboBoxItem Content="Tuvalet"/>
                    <ComboBoxItem Content="Lavabo"/>
                    <ComboBoxItem Content="Banyo"/>
                    <ComboBoxItem Content="Mutfak"/>
                    <ComboBoxItem Content="Çamaşırhane"/>
                    <ComboBoxItem Content="Temizlik"/>
                    <ComboBoxItem Content="Yağmur Suyu"/>
                </ComboBox>
                <TextBlock Text="Ara:" Margin="0,0,6,0"/>
                <TextBox x:Name="SearchBox" Width="180" TextChanged="SearchBox_Changed"/>
                <TextBlock x:Name="CountText" Margin="12,0,0,0" Foreground="#AAA" FontSize="11"/>
            </StackPanel>
        </Border>

        <!-- Ana: Liste + Detay -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="240"/>
            </Grid.ColumnDefinitions>

            <!-- Katalog Tablosu -->
            <DataGrid Grid.Column="0" x:Name="FixtureGrid" AutoGenerateColumns="False"
                      SelectionChanged="FixtureGrid_SelectionChanged"
                      MouseDoubleClick="FixtureGrid_DoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"        Binding="{Binding Id}"             Width="65"/>
                    <DataGridTextColumn Header="İsim (TR)" Binding="{Binding NameTR}"          Width="170"/>
                    <DataGridTextColumn Header="Kategori"  Binding="{Binding Category}"        Width="90"/>
                    <DataGridTextColumn Header="LU"        Binding="{Binding LoadUnit}"        Width="40"/>
                    <DataGridTextColumn Header="Soğuk DN"  Binding="{Binding MinColdWaterDN}"  Width="68"/>
                    <DataGridTextColumn Header="Sıcak DN"  Binding="{Binding MinHotWaterDN}"   Width="68"/>
                    <DataGridTextColumn Header="Pis Su DN" Binding="{Binding WasteDN}"         Width="68"/>
                    <DataGridTextColumn Header="Q (l/s)"   Binding="{Binding FlowRateLps}"     Width="58"/>
                    <DataGridTextColumn Header="Standart"  Binding="{Binding Standard}"        Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" Background="#333"/>

            <!-- Detay Paneli -->
            <Border Grid.Column="2" Background="#252535" CornerRadius="4" Padding="10">
                <StackPanel x:Name="DetailPanel">
                    <TextBlock Text="CİHAZ DETAYI" Foreground="#90CAF9" FontWeight="Bold" FontSize="11" Margin="0,0,0,8"/>
                    <TextBlock x:Name="DtlName"     Text="—" FontSize="14" FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,6"/>
                    <TextBlock x:Name="DtlId"       Text="" Foreground="#667" FontSize="10" Margin="0,0,0,8"/>
                    <Separator Background="#444" Margin="0,0,0,8"/>

                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/>
                            <RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Yük Birimi (LU):" Foreground="#90CAF9" FontSize="11"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="DtlLU" Text="—" FontWeight="Bold"/>
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Soğuk Su DN:" Foreground="#90CAF9" FontSize="11" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="DtlColdDN" Text="—" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Sıcak Su DN:" Foreground="#90CAF9" FontSize="11" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" x:Name="DtlHotDN" Text="—" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Pis Su DN:" Foreground="#90CAF9" FontSize="11" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" x:Name="DtlWasteDN" Text="—" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Debi (l/s):" Foreground="#90CAF9" FontSize="11" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="4" Grid.Column="1" x:Name="DtlFlow" Text="—" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="5" Grid.Column="0" Text="Sıcak Su:" Foreground="#90CAF9" FontSize="11" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="5" Grid.Column="1" x:Name="DtlHotReq" Text="—" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="6" Grid.Column="0" Text="Havalandırma:" Foreground="#90CAF9" FontSize="11" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="6" Grid.Column="1" x:Name="DtlVent" Text="—" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="7" Grid.Column="0" Text="Standart:" Foreground="#90CAF9" FontSize="11" Margin="0,4,0,0"/>
                        <TextBlock Grid.Row="7" Grid.Column="1" x:Name="DtlStd" Text="—" Margin="0,4,0,0"/>
                    </Grid>

                    <Separator Background="#444" Margin="0,10,0,8"/>

                    <!-- Yerleştirme pozisyonu -->
                    <TextBlock Text="YERLEŞTİRME KONUMU" Foreground="#90CAF9" FontWeight="Bold" FontSize="11" Margin="0,0,0,6"/>
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="30"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="X:"/>
                        <TextBox  Grid.Column="1" x:Name="TxtInsertX" Text="0"/>
                    </Grid>
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="30"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Y:"/>
                        <TextBox  Grid.Column="1" x:Name="TxtInsertY" Text="0"/>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Dönüş (°):" Margin="0,0,4,0"/>
                        <TextBox  Grid.Column="1" x:Name="TxtRotation" Text="0"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </DockPanel>
</Window>
