<UserControl x:Class="Afney.Cad.Presentation.Views.CadViewport"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:skia="clr-namespace:SkiaSharp.Views.WPF;assembly=SkiaSharp.Views.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#212830">
        <skia:SKElement x:Name="CadCanvas" 
                        PaintSurface="OnPaintSurface" 
                        MouseMove="CadCanvas_MouseMove" 
                        MouseDown="CadCanvas_MouseDown"
                        MouseUp="CadCanvas_MouseUp"
                        MouseWheel="CadCanvas_MouseWheel"
                        KeyDown="CadCanvas_KeyDown"
                        ContextMenuOpening="CadCanvas_ContextMenuOpening"
                        Focusable="True"
                        SnapsToDevicePixels="True"
                        UseLayoutRounding="True"
                        Cursor="Cross"
                        ToolTipService.InitialShowDelay="400"
                        ToolTipService.ShowDuration="5000">
            <!-- Dinamik ToolTip -->
            <skia:SKElement.ToolTip>
                <ToolTip x:Name="EntityToolTip" Background="#333" Foreground="White" BorderBrush="#555" BorderThickness="1" Visibility="Collapsed">
                    <StackPanel x:Name="EntityToolTipContent" />
                </ToolTip>
            </skia:SKElement.ToolTip>
            <!-- AutoCAD Tarzı Sağ Tıklama Menüsü -->
            <skia:SKElement.ContextMenu>
                <ContextMenu Background="#2B2B2C" BorderBrush="#555" BorderThickness="1" Padding="4" Foreground="White">
                    <ContextMenu.Resources>
                        <Style TargetType="MenuItem">
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Padding" Value="6,4"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="FontFamily" Value="Segoe UI"/>
                        </Style>
                        <Style TargetType="Separator">
                            <Setter Property="Background" Value="#444"/>
                            <Setter Property="Margin" Value="4,2"/>
                        </Style>
                    </ContextMenu.Resources>
                    <MenuItem x:Name="CtxMenu_Move" Header="Taşı" Click="OnContextMenu_Move" InputGestureText="M"/>
                    <MenuItem x:Name="CtxMenu_Delete" Header="Sil" Click="OnContextMenu_Delete" InputGestureText="Del"/>
                    <MenuItem x:Name="CtxMenu_Mirror" Header="Aynala" Click="OnContextMenu_Mirror" InputGestureText="MI"/>
                    <MenuItem x:Name="CtxMenu_Rotate" Header="Döndür" Click="OnContextMenu_Rotate"/>
                    <MenuItem x:Name="CtxMenu_Scale" Header="Ölçekle" Click="OnContextMenu_Scale"/>
                    <MenuItem x:Name="CtxMenu_Stretch" Header="Esnet" Click="OnContextMenu_Stretch"/>
                    <Separator/>
                    <MenuItem x:Name="CtxMenu_GripPoint" Header="Tutma Noktası" Click="OnContextMenu_GripPoint"/>
                    <MenuItem x:Name="CtxMenu_Copy" Header="Kopyala" Click="OnContextMenu_Copy" InputGestureText="CO"/>
                    <MenuItem x:Name="CtxMenu_Undo" Header="Geri Al" Click="OnContextMenu_Undo" InputGestureText="Ctrl+Z"/>
                    <Separator/>
                    <MenuItem x:Name="CtxMenu_Properties" Header="Özellikler..." Click="OnContextMenu_Properties"/>
                    <Separator/>
                    <MenuItem x:Name="CtxMenu_Pan" Header="Pan (Kaydır)" Click="OnContextMenu_Pan"/>
                    <MenuItem x:Name="CtxMenu_ZoomExtents" Header="Zoom Extents" Click="OnContextMenu_ZoomExtents"/>
                    <MenuItem x:Name="CtxMenu_SelectAll" Header="Tümünü Seç" Click="OnContextMenu_SelectAll" InputGestureText="Ctrl+A"/>
                    <MenuItem x:Name="CtxMenu_ClearSelection" Header="Seçimi Temizle" Click="OnContextMenu_ClearSelection"/>
                </ContextMenu>
            </skia:SKElement.ContextMenu>
        </skia:SKElement>
        
        <!-- Viewport Overlay Debug/Info -->
        <Border Background="#44000000" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="10" Padding="5" CornerRadius="3">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="CoordinateText" Text="X: 0, Y: 0" Foreground="Lime" FontSize="10" FontFamily="Consolas"/>
                <Separator Margin="5,0" Background="#666"/>
                <TextBlock x:Name="ZoomText" Text="Z: 1.0" Foreground="Orange" FontSize="10" FontFamily="Consolas"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
